networks:
  tdl:
    driver: bridge

services:
  tdl_api:
    image: node:latest
    container_name: tdl_bdd
    networks:
      - tdl
    working_dir: /usr/src/app
    ports:
      - "3001:3001"
    volumes:
      - ./:/usr/src/app
    command: node index.js

